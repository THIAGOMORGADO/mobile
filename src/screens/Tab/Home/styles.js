import { COLORS, FONT_SIZE } from "../../../constants/theme";

export const styles = {
  container: {
    flex: 1,
    backgroundColor: COLORS.white,
    padding: 12,
  },
  title: {
    fontSize: FONT_SIZE.md,
    color: COLORS.gray1,
    marginBottom: 15,
    marginLeft: 15,
  }
}