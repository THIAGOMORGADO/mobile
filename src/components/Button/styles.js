import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
  btnContainer: {
    width: "100%",
    backgroundColor: COLORS.blue,
    borderRadius: 6,
    padding: 12,
  },
  btnText: {
    textAlign: "center",
    color: COLORS.white,
    fontWeight: "bold",
    fontSize: FONT_SIZE.md
  },
}